# Git commits

- Commit messages: short, single line, imperative mood. No body unless truly necessary.
- Never add a `Co-Authored-By` line.
- Only commit when explicitly asked.

# Git branches

- Do not create branches.
