#!/bin/bash
# Installed via scripts/install-git-hooks.sh (git config core.hooksPath .githooks).
# Blocks the push if the local CI pipeline fails.
set -e

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$repo_root/scripts/local-ci.sh"
